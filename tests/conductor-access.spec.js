/**
 * Reservado: acceso conductor / flujos en Unidades.
 * Implementar cuando el alcance de QA lo requiera.
 */

const { test } = require('@playwright/test');

test.describe.skip('P2L — Conductor (pendiente)', () => {
  test('placeholder', async () => {
    // Intencionalmente vacío.
  });
});
